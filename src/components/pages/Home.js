import React, { Fragment } from 'react';
import { Alert } from '../layout/Alert';
import { Hero } from '../layout/Hero';

export const Home = () => {
	return (
		<Fragment>
			<Alert />
			<Hero />
		</Fragment>
	);
};
